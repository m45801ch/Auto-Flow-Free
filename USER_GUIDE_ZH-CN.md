# Auto Flow Free 使用说明（简体中文）

**Auto Flow Free**（原名 Flow Automation）是一款 Chrome 扩展程序，用于在 Google Flow（labs.google/fx/tools/flow）上自动批量生成视频与图片，并自动下载成果。您只需准备好提示词（Prompt）清单，扩展就会替您完成创建项目、填写内容、提交任务、跟踪进度与下载成果的全部流程，无需手动逐一操作。

---

## 主要功能

| 功能 | 说明 |
| --- | --- |
| 队列支持 | 一次加入多个提示词，自动依次处理，无需手动逐个操作。 |
| 文字转视频 | 从文字描述批量生成视频，支持导入 .txt、.xlsx、.csv 文件。 |
| 帧转视频 | 以静态图片为起点生成视频，支持每段使用起始帧或首尾帧。 |
| 组件转视频 | 让角色或界面组件动起来，支持按文件名自动匹配角色图片。 |
| 文字转图片 | 从文字描述批量生成图片。 |
| 图片转图片 | 以 AI 按提示词转换与优化图片。 |
| 智能体自动化 | 以 Google Flow 的智能体控制执行自动化任务。 |
| 连锁生成（Chain Prompt） | 自动将上一段视频的最后一帧作为下一段的输入图片，串起整条故事链。 |
| 断点续跑 | 意外中断后可从断点继续执行，每段最后一帧副本会保留。 |
| 实时预览面板 | 实时查看连锁图片与视频，可拖拽排序、手动替换输入帧、自动检测色彩过渡并在失败时自动重试。 |
| 一键导出项目 | 将整条连锁（提示词设置、分段视频、最后一帧图片）打包为 ZIP 下载。 |
| 自动下载 | 视频（720p／1080p／4K）与图片（1K／2K／4K）自动下载，可按项目建立文件夹整理。 |
| 旁白稿批量导入 | 导入 .txt／.csv 旁白稿并自动对应各段提示词，内置 30 个 Google Chirp 3 HD 语音。 |
| 角色与语音自动匹配 | 按文件名与分词匹配引擎自动添加角色图片；提示词提到说话者名称时自动选择对应语音。 |
| 调试日志报告 | 实时记录每段任务状态并正确显示日志条数，支持自动滚动、复制、清除与导出（TXT）功能，失败项以红色标注。 |
| 侧边面板 | 点击工具栏图标以浏览器右侧分割画面打开，宽度可用鼠标拖拽调整。 |
| 双主题界面 | 明亮普罗旺斯（粉黑白）为默认主题，可切换至暗黑（绿黑白），所有文字对比清晰。 |
| 多语言界面 | 简体中文、繁体中文、English 三种语言，在「设置」标签页以语言下拉菜单切换（与右上角整合为单一控制）。 |
| GitHub 仓库 | 右上角「GitHub」按钮打开项目仓库（新标签页）。 |
| 顶部版本号 | 界面最上方显示当前版本徽章（如 v1.9.1），与 GitHub 按钮并排，方便确认版本。 |
| 多语言完整覆盖 | 三种语言界面的全部文字（含下拉选项、提示、按钮提示）均来自语言字典，无残留外语；英文界面已全面验证无中文字残留。 |

---

## 安装方式

本扩展为开发者版，请按以下步骤安装：

1. 下载并解压 `flow-automation.zip`。
2. 在 Chrome 浏览器中打开 `chrome://extensions`。
3. 打开右上角「**开发者模式**」。
4. 点击「**加载已解压的扩展程序**」，选择解压后的文件夹。
5. 加载完成后，建议点击扩展卡片上的「**重新加载**」图标以确保使用最新版本。

安装后请在 Chrome 工具栏点击拼图图标，将 Auto Flow Free **固定**以便使用。

---

## 快速开始

1. **打开 Google Flow**：前往 [Google Flow](https://labs.google/fx/tools/flow) 并创建或打开一个项目。本扩展仅在 Flow 项目页面上可用；若不在 Flow 页面，界面会显示提示框并锁定操作，切回 Flow 后自动解除。
2. **打开扩展**：点击 Chrome 工具栏的 Auto Flow Free 图标，界面会以右侧分割画面打开。
3. **选择模式**：在「控制」标签页中选择生成模式（共六种）。
4. **填入提示词**：在输入框粘贴提示词（每段以空行分隔），或导入文件。
5. **点击「运行」**：开始批量处理，请勿关闭 Flow 页面。

---

## 六种生成模式

### 1. 文字转视频

1. 在「控制」标签页选择「文字转视频」模式。
2. 在输入框粘贴提示词（每段以空行分隔），或点击「上传 .txt 文件」、「上传 .xlsx / .csv」导入。
3. 在「设置」标签页调整视频时长（4 秒／6 秒／8 秒／10 秒／4 秒合并／6 秒合并）。
4. 点击「**运行**」开始批量生成。

> **提示词示例**（两段提示词，中间以空行分隔）：
> ```
> 平静海面上美丽的夕阳，浪花轻柔，
> 镜头缓慢横向扫过地平线。
>
> 夜晚霓虹灯下的繁忙城市街道，
> 车辆与行人穿梭于画面之中。
> ```

### 2. 帧转视频

1. 选择「帧转视频」模式。
2. 点击上传区域选择图片（可多张）。
3. 为每张图片输入提示词（以空行分隔）。
4. 在「设置」标签页设置每段使用「仅使用起始帧」或「使用首尾帧」。
5. 点击「运行」开始处理。

### 3. 组件转视频

1. 选择「组件转视频」模式。
2. 上传角色／组件图片。
3. 输入描述动画的提示词。
4. 可启用「自动添加角色图片」（按文件名自动匹配），并搭配「扫描角色」同步项目中的角色。
5. 可启用「按说话者自动添加语音」，提示词提到说话者名称时自动选定语音，并可设置「默认说话者」。
6. 可设置每个提示词的最大输入图片数（1–10 张）与每个提示词的输出数量。
7. 点击「运行」开始生成。

### 4. 文字转图片

1. 选择「文字转图片」模式。
2. 输入详细的图片描述（每段以空行分隔）。
3. 在「设置」标签页调整宽高比（16:9／9:16／1:1／3:4／4:3）与图像模型。
4. 点击「运行」开始生成。

### 5. 图片转图片

1. 选择「图片转图片」模式。
2. 上传来源图片。
3. 输入转换提示词。
4. 可启用「自动添加角色图片」（按文件名自动匹配图片）。
5. 点击「运行」开始生成。

### 6. 智能体自动化

1. 选择「智能体自动化」模式。
2. 输入描述智能体指令的提示词。
3. 支持角色控制与按说话者自动添加语音。
4. 点击「运行」开始执行智能体任务。

---

## 连锁生成（Chain Prompt）

连锁生成本扩展的核心特色。启用后，系统会自动将上一段视频的最后一帧画面作为下一段提示词的输入图片，让您可以用多段短视频串出一条连贯的长故事。

**连锁预览实时管理面板**会在运行中自动生成每段的最后一帧图片副本与已生成视频播放。您可以：

- **拖拽卡片**调整段落顺序；
- 点击「**替换帧**」手动更换某一段的输入图片；
- 系统会**自动检测前后段之间的色彩过渡**，若过渡不连续会自动重试；
- 支持「**断点续跑**」：中断后从最后完成段继续，无需从头来过；
- 完成后点击「**导出项目**」，将整条连锁（提示词设置、分段视频、最后一帧图片）打包为 ZIP 下载。

---

## 旁白稿批量导入

将旁白稿存为 .txt 或 .csv 文件后，点击「导入旁白稿」即可批量导入，系统会自动依序将每段旁白对应到各段提示词。系统内置 30 个 Google Chirp 3 HD 语音可供「默认说话者」下拉菜单选用；启用「按说话者自动添加语音」后，提示词中出现的说话者名称会自动匹配对应语音。

---

## 设置标签页

### 常规设置

| 选项 | 说明 |
| --- | --- |
| 默认模式 | 创建新视频时的默认生成模式。 |
| 默认宽高比 | 下拉菜单：16:9／9:16／1:1／3:4／4:3 五种比例可选。 |
| 每个提示词的输出数量 | 每个提示词生成的图片／视频数量。 |
| 并发 Prompt | 同时处理 1–6 个提示词（合并模式以单任务执行）。 |
| 随机等待 | 任务间加入随机等待时间，避免触发速率限制。 |

### 模型与时长

| 选项 | 说明 |
| --- | --- |
| 模型（视频） | Veo 3.1 Lite／Lite [Lower Priority]／Fast／Quality／Omni Flash（需 Pro、Ultra 方案）等。 |
| 图像模型 | Nano Banana Pro／Nano Banana 2／Nano Banana 2 Lite。 |
| 默认视频选项 | 4 秒／6 秒／8 秒／10 秒／4 秒合并（Ultra）／6 秒合并（Ultra）。 |
| 默认图片模式 | 新图片／上一张图片。 |
| 最大输入图片数 | 每段提示词最多使用的输入图片数量（1–10 张）。 |

### 下载与高级设置

| 选项 | 说明 |
| --- | --- |
| 自动下载质量（视频） | 720p／1080p（需 Ultra／Pro 方案）／4K（需 Ultra／Pro 方案）。 |
| 自动下载质量（图片） | 不下载／1K／2K／4K（需 Ultra 方案）。 |
| 保存到文件夹 | 指定 Chrome 下载文件夹内的子文件夹名称，自动按项目整理成果。 |
| 自动重命名文件 | 自动以清晰的前缀与项目路径重命名下载文件。 |
| 去除水印 | 提供前往去除水印网站的链接（在新标签页打开）。 |
| 主题 | 明亮普罗旺斯（粉黑白）为默认，可切换至暗黑（绿黑白）。 |
| 宽高比 | 下拉菜单：16:9／9:16／1:1／3:4／4:3。 |
| 语言 | 简体中文／繁体中文／English，下拉菜单切换。 |

**下载设置**区块说明视频将下载到 Chrome 的下载文件夹，每个项目会有自己的文件夹来存储视频；点击齿轮图标可快速打开 Chrome 下载设置。所有设置会自动在所有浏览器标签页中同步。

---

## 其他功能

- **调试日志报告标签页**：记录每段任务的执行状态（上传、成功、失败、重试），正确显示日志条数，提供自动滚动、复制、清除与一键导出（TXT 文件，自动带时间戳）功能，失败项以红色标注。
- **非 Flow 页面提示**：不在 Flow 项目页面时会弹出提示框并锁定操作，切回 Flow 页面后自动消失、功能恢复；支持 Flow 的所有语言版本（繁体、简体、英文等）。
- **清除缓存／报告错误**：遇到异常活动错误时，可点击「清除缓存」尝试修复。

---

## 疑难排解

| 问题 | 原因与解决方法 |
| --- | --- |
| 扩展无法使用 | 确认您位于 Google Flow 项目页面（`labs.google/fx/tools/flow/project/...`，含所有语言版本）。 |
| 异常活动／验证错误 | 1. 点击「控制」标签页的「清除缓存」按钮。2. 尝试创建新项目。3. 尝试使用新的浏览器配置文件。4. 确认手动操作可正常提交多个提示词。 |
| 视频／图片未下载 | 关闭 Chrome 的下载询问：Chrome 设置 → 下载内容 → 关闭「下载前询问每个文件的保存位置」。 |
| 政策错误 | Google 判定提示词或图片违规，工具会自动跳过并继续下一项任务。 |
| 画面缩小 | 运行中扩展会自动缩小画面以定位按钮，请勿手动调整浏览器缩放比例。 |

---

## 隐私与数据

所有任务完全在您的浏览器沙盒内执行，提示词、素材与设置仅存储于本机 Chrome 本地存储（用于跨标签页同步设置），不会发送至任何外部服务器。

---

*本扩展为独立项目，与 Google 或 Google Flow 团队无关联、未获其背书或支持。*

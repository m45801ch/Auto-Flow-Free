# Auto Flow Free 使用說明（繁體中文）

**Auto Flow Free**（原名 Flow Automation）是一款 Chrome 擴充功能，用於在 Google Flow（labs.google/fx/tools/flow）上自動批次生成影片與圖片，並自動下載成果。您只需準備好提示詞（Prompt）清單，擴充功能就會替您完成建立專案、填寫內容、送出任務、追蹤進度與下載成果的全部流程，無須手動逐一操作。

---

## 主要功能

| 功能 | 說明 |
| --- | --- |
| 佇列支援 | 一次加入多個提示詞，自動依序處理，無須手動逐個操作。 |
| 文字轉影片 | 從文字描述批次生成影片，支援匯入 .txt、.xlsx、.csv 檔案。 |
| 幀轉影片 | 以靜態圖片為起點生成影片，支援每段使用開始幀或首尾幀。 |
| 組件轉影片 | 讓角色或介面組件動起來，支援依檔名自動匹配角色圖片。 |
| 文字轉圖片 | 從文字描述批次生成圖片。 |
| 圖片轉圖片 | 以 AI 依提示詞轉換與優化圖片。 |
| 智慧體自動化 | 以 Google Flow 的智慧體控制執行自動化任務。 |
| 連鎖生成（Chain Prompt） | 自動將上一段影片的最後一幀作為下一段的輸入圖片，串起整條故事鏈。 |
| 斷點續跑 | 意外中斷後可從斷點繼續執行，每段最後幀副本會保留。 |
| 即時預覽面板 | 即時檢視連鎖圖片與影片，可拖曳排序、手動替換輸入幀、自動偵測色彩過渡並在失敗時自動重試。 |
| 一鍵匯出專案 | 將整條連鎖（提示詞設定、分段影片、最後幀圖片）打包為 ZIP 下載。 |
| 自動下載 | 影片（720p／1080p／4K）與圖片（1K／2K／4K）自動下載，可依專案建立資料夾整理。 |
| 語音自動套用 | 內建 Google 語音模組可供「預設說話者」下拉選單選用；啟用「自動套用語音」後，擴充自動選擇語音並填入 Flow 的語音設定欄位，提示詞提到說話者名稱時自動選擇對應語音。 |
| 角色與語音自動匹配 | 依檔名與分詞匹配引擎自動新增角色圖片；提示詞提到說話者名稱時自動選擇對應語音。 |
| 調試日誌報告 | 即時記錄每段任務狀態並正確顯示日誌條數，支援自動滾動、複製、清除與匯出（TXT）功能，失敗項目以紅色標示。 |
| 側邊面板 | 點擊工具列圖示以瀏覽器右側分割畫面開啟，寬度可用滑鼠拖曳調整。 |
| 雙主題介面 | 明亮普羅旺斯（粉黑白）為預設主題，可切換至暗黑（綠黑白），所有文字對比清晰。 |
| 多語言介面 | 繁體中文、简体中文、English 三種語言，於「設定」頁籤以語言下拉選單切換（與右上角整合為單一控制）。 |
| GitHub 倉庫 | 右上角「GitHub」按鈕開啟專案倉庫（新分頁）。 |
| 頂部版本號 | 界面最上方顯示目前版本徽章（如 v1.9.1），與 GitHub 按鈕並列，方便確認版本。 |
| 多語言完整覆蓋 | 三種語言界面全部文字（含下拉選項、提示、按鈕提示）均來自語言字典，無殘留外語；英文界面已全面驗證無中文殘留。 |

---

## 安裝方式

本擴充功能為開發者版，請依以下步驟安裝：

1. 下載並解壓縮 `flow-automation.zip`。
2. 在 Chrome 瀏覽器開啟 `chrome://extensions`。
3. 開啟右上角「**開發人員模式**」。
4. 點擊「**載入已解壓縮的擴充功能**」，選擇解壓縮後的資料夾。
5. 載入完成後，建議點擊擴充功能上的「**重新載入**」圖示以確保使用最新版本。

安裝後請在 Chrome 工具列點擊拼圖圖示，將 Flow Automation **釘選**起來方便使用。

---

## 快速開始

1. **開啟 Google Flow**：前往 [Google Flow](https://labs.google/fx/tools/flow) 並建立或開啟一個專案。本擴充功能僅在 Flow 專案頁面上可用；若不在 Flow 頁面，介面會顯示提示框並鎖定操作，切回 Flow 後自動解除。
2. **開啟擴充功能**：點擊 Chrome 工具列的 Auto Flow Free 圖示，介面會以右側分割畫面開啟。
3. **選擇模式**：在「控制」頁籤中選擇生成模式（共六種）。
4. **填入提示詞**：在輸入框貼上提示詞（每段以空行分隔），或匯入檔案。
5. **點擊「執行」**：開始批次處理，請勿關閉 Flow 頁面。

---

## 六種生成模式

### 1. 文字轉影片

1. 在「控制」頁籤選擇「文字轉影片」模式。
2. 於輸入框貼上提示詞（每段以空行分隔），或點擊「匯入提示詞 .txt / .csv」匯入檔案。
3. 在「設定」頁籤調整影片長度（4 秒／6 秒／8 秒／10 秒／4 秒合併／6 秒合併）。
4. 點擊「**執行**」開始批次生成。

> **提示詞範例**（兩段提示詞，中間以空行分隔）：
> ```
> 清晨的山谷裡，一群白色綿羊緩緩走過懸索橋，薄霧繚繞，
> 鏡頭沿橋面向前緩慢推進。
>
> 午後的舊鐵路上，一輛復古蒸汽火車鳴笛駛過彎道，
> 金黃陽光灑在車廂上，揚起淡淡塵土。
> ```

### 2. 幀轉影片

1. 選擇「幀轉影片」模式。
2. 點擊上傳區域選擇圖片（可多張）。
3. 為每張圖片輸入提示詞（以空行分隔）。
4. 在「設定」頁籤設定每段使用「僅使用開始幀」或「使用首尾幀」。
5. 點擊「執行」開始處理。

### 3. 組件轉影片

1. 選擇「組件轉影片」模式。
2. 上傳角色／組件圖片。
3. 輸入描述動畫的提示詞。
4. 可啟用「自動新增角色圖片」（依檔名自動匹配），並搭配「掃描角色」同步專案中的角色。
5. 可啟用「按說話者自動新增語音」，提示詞提到說話者名稱時自動選定語音，並可設定「預設說話者」。
6. 可設定每個提示詞的最大輸入圖片數（1–10 張）與每個提示詞的輸出數量。
7. 點擊「執行」開始生成。

### 4. 文字轉圖片

1. 選擇「文字轉圖片」模式。
2. 輸入詳細的圖片描述（每段以空行分隔）。
3. 在「設定」頁籤調整寬高比（16:9／9:16／1:1／3:4／4:3）與圖像模型。
4. 點擊「執行」開始生成。

### 5. 圖片轉圖片

1. 選擇「圖片轉圖片」模式。
2. 上傳來源圖片。
3. 輸入轉換提示詞。
4. 可啟用「自動新增角色圖片」（依檔名自動匹配圖片）。
5. 點擊「執行」開始生成。

### 6. 智慧體自動化

1. 選擇「智慧體自動化」模式。
2. 輸入描述智慧體指令的提示詞。
3. 支援角色控制與按說話者自動新增語音。
4. 點擊「執行」開始執行智慧體任務。

---

## 連鎖生成（Chain Prompt）

連鎖生成是本擴充的核心特色。啟用後，系統會自動將上一段影片的最後一幀畫面作為下一段提示詞的輸入圖片，讓您可以用多段短影片串出一條連貫的長故事。

**連鎖預覽即時管理面板**會在執行中自動生成每段的最後幀圖片副本與已生成影片播放。您可以：

- **拖曳卡片**調整段落順序；
- 點擊「**替換幀**」手動更換某一段的輸入圖片；
- 系統會**自動偵測前後段之間的色彩過渡**，若過渡不連續會自動重試；
- 支持「**斷點續跑**」：中斷後從最後完成段繼續，不用重頭來過；
- 完成後點擊「**匯出專案**」，將整條連鎖（提示詞設定、分段影片、最後幀圖片）打包為 ZIP 下載。

---

## 語音自動套用

此設定區塊控制的是**語音欄位的自動填寫**（與角色對話的聲音無關，角色聲音由 Veo 自動生成，男女聲依角色自動配對）。

系統內建 Google 語音模組可供「預設說話者」下拉選單選用；啟用「**自動套用語音（按說話者自動選語音）**」後，擴充會自動選擇語音並填入 Flow 的語音設定欄位：提示詞提到說話者名稱時自動選擇對應語音，否則使用預設說話者（選「無」則未命中的段落不設定語音，由 Flow 依內建行為處理）。若某段不想套用語音，可在該段提示詞加入 `[NOVOICE]`，該段將不填入語音設定，由 Veo 自動生成的角色聲音接管；標籤會於送出前自動清除，不會出現在影片中。

---

## 設定頁籤

### 一般設定

| 選項 | 說明 |
| --- | --- |
| 預設模式 | 建立新影片時的預設生成模式。 |
| 預設寬高比 | 下拉選單：16:9／9:16／1:1／3:4／4:3 五種比例可選。 |
| 每個提示詞的輸出數量 | 每個提示詞生成的圖片／影片數量。 |
| 並行 Prompt | 同時處理 1–6 個提示詞（合併模式以單任務執行）。 |
| 隨機等待 | 任務間加入隨機等待時間，避免觸發速率限制。 |

### 模型與長度

| 選項 | 說明 |
| --- | --- |
| 模型（影片） | Veo 3.1 Lite／Lite [Lower Priority]／Fast／Quality／Omni Flash（Pro、Ultra 方案）等。 |
| 圖像模型 | Nano Banana Pro／Nano Banana 2／Nano Banana 2 Lite。 |
| 預設影片選項 | 4 秒／6 秒／8 秒／10 秒／4 秒合併（Ultra）／6 秒合併（Ultra）。 |
| 預設圖片模式 | 新圖片／上一張圖片。 |
| 最大輸入圖片數 | 每段提示詞最多使用的輸入圖片數量（1–10 張）。 |

### 下載與進階設定

| 選項 | 說明 |
| --- | --- |
| 自動下載品質（影片） | 720p／1080p（需 Ultra／Pro 方案）／4K（需 Ultra／Pro 方案）。 |
| 自動下載品質（圖片） | 不下載／1K／2K／4K（需 Ultra 方案）。 |
| 儲存到資料夾 | 指定 Chrome 下載資料夾內的子資料夾名稱，自動整理成果。 |
| 自動重新命名檔案 | 自動以清楚的前綴與專案路徑重新命名下載檔案。 |
| 移除浮水印 | 提供前往去除浮水印網站的連結（於新分頁開啟）。 |
| 主題 | 明亮普羅旺斯（粉黑白）為預設，可切換至暗黑（綠黑白）。 |
| 寬高比 | 下拉選單：16:9／9:16／1:1／3:4／4:3。 |
| 語言 | 繁體中文／简体中文／English，下拉選單切換。 |

**下載設定**區塊說明影片將下載到 Chrome 的下載資料夾，每個專案會有自己的資料夾來儲存影片；點擊齒輪圖示可快速開啟 Chrome 下載設定。所有設定會自動在所有瀏覽器分頁中同步。

---

## 其他功能

- **調試日誌報告頁籤**：記錄每段任務的執行狀態（上傳、成功、失敗、重試），正確顯示日誌條數，提供自動滾動、複製、清除與一鍵匯出（TXT 檔案，自動帶時間戳）功能，失敗項目以紅色標示。
- **非 Flow 頁面提示**：不在 Flow 專案頁面時會彈出提示框並鎖定操作，切回 Flow 頁面後自動消失、功能恢復；支援 Flow 的所有語言版本（繁體、簡體、英文等）。
- **清除快取／回報錯誤**：遇到異常活動錯誤時，可點擊「清除快取」嘗試修復。

---

## 疑難排解

| 問題 | 原因與解決方法 |
| --- | --- |
| 擴充功能無法使用 | 確認您位於 Google Flow 專案頁面（`labs.google/fx/tools/flow/project/...`，含所有語言版本）。 |
| 異常活動／驗證錯誤 | 1. 點擊「控制」頁籤的「清除快取」按鈕。2. 嘗試建立新專案。3. 嘗試使用新的瀏覽器設定檔。4. 確認手動操作可正常送出多個提示詞。 |
| 影片／圖片未下載 | 關閉 Chrome 的下載詢問：Chrome 設定 → 下載項目 → 關閉「下載每個檔案前先詢問儲存位置」。 |
| 政策錯誤 | Google 判定提示詞或圖片違規，工具會自動跳過並繼續下一項任務。 |
| 畫面縮小 | 執行中擴充功能會自動縮小畫面以定位按鈕，請勿手動調整瀏覽器縮放比例。 |

---

## 隱私與資料

所有任務完全在您的瀏覽器沙盒內執行，提示詞、素材與設定僅儲存於本機 Chrome 本機儲存空間（用於跨分頁同步設定），不會傳送至任何外部伺服器。

---

*本擴充功能為獨立專案，與 Google 或 Google Flow 團隊無關聯、未獲其背書或支援。*
